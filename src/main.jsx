import React from 'react'
import ReactDOM from 'react-dom/client'
import UTMAttributionFlow from '../flowchart.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <UTMAttributionFlow />
  </React.StrictMode>,
)
